[project]
name = "semantic-layer-duckdb"
version = "0.1.0"
description = "Trying boring semantic layer with DuckDB"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pip>=26.1.2",
]
